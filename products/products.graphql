type Query {
  products: [Product]
}

type Product {
  id: ID!
  name: String!
  description: String!
  price: Float!
  reviews: [Review]
}

type Review {
  rating: Int!
  comment: String
}
